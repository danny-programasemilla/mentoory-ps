@{
    ViewData["Title"] = "Plantillas de Diagnóstico";
}

@await Component.InvokeAsync("DataTable", new { tableId = "templatesTable", columns = new[] { "Nombre", "Descripción", "Nivel de Suscripción", "Versión", "Estado", "Fecha de Creación" } })

@section Scripts {
    <script src="~/js/datatable-helper.js"></script>
    <script>
        document.addEventListener('DOMContentLoaded', function () {
            initDataTable('templatesTable', {
                apiUrl: '@Url.Action("DiagnosticsData", "Templates", new { area = "Platform" })',
                columns: [
                    { data: 'name' },
                    {
                        data: 'description',
                        render: function (data) {
                            return data || '<span class="text-muted">Sin descripción</span>';
                        }
                    },
                    {
                        data: 'subscriptionTier',
                        render: function (data) {
                            return data || '<span class="text-muted">Todas</span>';
                        }
                    },
                    { data: 'version' },
                    {
                        data: 'isActive',
                        render: function (data) {
                            return data
                                ? renderStatus('Activa', 'success')
                                : renderStatus('Inactiva', 'secondary');
                        }
                    },
                    {
                        data: 'createdAtUtc',
                        render: function (data) { return formatDate(data); }
                    }
                ],
                filters: [{ column: 'isActive', type: 'select', options: FILTER_OPTIONS_ACTIVE_FEM }],
                defaultOrder: [[5, 'desc']]
            });
        });
    </script>
}
